# ci-cd-demo

A tiny sandbox repo for learning GitHub Actions and CI/CD.

Some config files in this repo still have unfilled placeholder
values (anything starting with `#INPUT_`). Your job today is to
write a GitHub Actions workflow that automatically catches those
before they'd ever get merged.

Nothing here needs to run — you're just checking that no
`#INPUT_` placeholders got left behind.